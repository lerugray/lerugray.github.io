# 2026-04-18 — session 05: landing anchor + Infrastructure section + GS/RB pages

**Working session type:** interactive, Ray + Claude Opus 4.7 (1M context).
**Goal:** use Claude Design to produce a single landing-page anchor, port to Astro, hand-extend vocabulary, add two new Infrastructure routes (GeneralStaff, RayBrain) that didn't exist before. Apply the Addendum-3 discipline from GeneralStaff (one anchor brief; hand-extend everything else; decline CD follow-ups).
**Machine:** Ray's desk.

## Opening state

Fresh from session 4 (2026-04-16). Thirteen pages live at `lerugray.github.io`. Design system v1 locked, eight open-item polish tasks from the handoff. CLAUDE.md still said "scaffolding stage, no site code written" despite Phases 17–21 + Session 4 having shipped — stale, fixed this session.

## Work done

### Advisor-first pass before burning CD budget

Ray proposed using Claude Design for a landing redesign + adding RayBrain + GeneralStaff to the site. Before writing a brief, surfaced four scoping questions:

1. **Reboot or refine?** Refine — keep the locked warm-dark / Plex / grain system; the home's weakness (if any) is hierarchy, not palette.
2. **Which page is the anchor?** `/` home, confirmed.
3. **Anchor-propagation model.** Extract `:root` + component vocabulary from CD output, hand-extend downstream with zero additional CD calls per Addendum-3.
4. **RayBrain + GeneralStaff placement.** Ray's `raybrain/CLAUDE.md §0.1` explicitly names both as "production infrastructure for parallelizing work across multiple thin-margin creative/business projects" — pushed back on folding into Projects (dilutes curation) or Businesses (misrepresents them as products for sale). Proposed **Infrastructure** as a new top-level taxonomy; Ray confirmed.

### Claude Design anchor

Wrote a single-shot brief pinning the locked design system token-by-token, calling out the generic-AI-portfolio failure mode explicitly, and listing eight patterns the anchor had to solve in one pass (hero / bio / featured-essay / section-head / editorial-entries / infrastructure-entries / music-tile / colophon). Ray attached current-home + Hammerstein-essay + CSL-umbrella screenshots + wargame-counter PNG + grain SVG, ran the brief, produced [`docs/phase-22-anchor/landing.html`](../../docs/phase-22-anchor/landing.html). Budget spent: one brief. No follow-ups accepted.

### Audit — six flags at port time

CD's output passed the hardest rule (accent scarcity: exactly two uses) but drifted on six minor points:

1. New `#7ba05b` olive status dot — rejected; swapped to `var(--color-link)` amber for live, `var(--color-subtle)` gray for private. **No new palette colors admitted.**
2. `translateX(3px)` transform on featured-CTA arrow — removed (no-motion rule).
3. Inline `onclick` for mobile hamburger — replaced with existing horizontal-scroll mobile topbar (zero-JS rule held).
4. `/essays/hammerstein` + `/conflict-simulations` route URLs — corrected to `/writing/hammerstein` + `/conflict-simulations-llc`.
5. "Listen →" CTA on Music — kept (functional, no slop).
6. "04 ——" editorial section counts — kept (distinctive, reinforces Plex Mono texture, structural-truth signal).

### Port to Astro

Four files changed:

- **`src/styles/global.css`** — appended the anchor's component vocabulary as named classes (`.hero`, `.bio`, `.featured`, `.entries`, `.infra-list`, `.infra-meta`, `.section-head`, `.section-label`, `.section-count`, `.section-subhead`, `.music-block`, `.nav-group`). Tokens stay in existing `@theme` block; no new color tokens admitted.
- **`src/data/navigation.ts`** — flat `navItems[]` replaced by grouped `navGroups[]` (Writing / Businesses / Infrastructure / Projects / Music).
- **`src/components/SiteNav.astro`** — sidebar variant renders groups with `.nav-group-label` dividers; topbar variant flattens `navGroups` for the existing horizontal-scroll mobile pattern.
- **`src/pages/index.astro`** — full rewrite using the anchor's eight patterns with the actual copy from the brief.

Build clean: 13 → 15 pages, 0 errors, 0 warnings.

### Two new routes — `/generalstaff` and `/raybrain`

**`/generalstaff`** — full project page pulled from the public `GeneralStaff/README.md` and `LAUNCH-PLAN.md`. Includes the pitch ("Open-source autonomous engineering for solo founders."), the sub-tagline ("Your code. Your keys. Your control."), the infra-meta status strip (Status / License / Access), the problem + approach narrative, the 10 Hard Rules, the Hammerstein framing with cross-link to `/writing/hammerstein`, and the phase roadmap (1–4 shipped, 5 in progress, 7 = public launch). Ray's own framing verbatim: *"my scheme to democratize Polsia while keeping four businesses running sanely."* The word "sanely" is the Ray-voice hook — kept.

**`/raybrain`** — full project page, **architectural content only**. Covers the Karpathy three-layer LLM-Wiki pattern, the four enforced invariants (citation floor / idempotent regeneration / user-editable user-rejectable / query-time co-visibility), the citation-grammar `[ref:...]` format, the refusal stance ("synthesis is out of scope by design — read the passages and decide"), and the relationship to GeneralStaff (sibling infrastructure, same Hammerstein thesis from different angles: execution verification vs. retrieval refusal).

**IP boundary held.** Nothing from `raybrain/CLAUDE.md §0` (political identity, AI-use philosophy prose, project motivation) surfaced. Corpus contents never named. Implementation-churn details (Voyage embeddings, Claude CLI synthesis vs. Ollama) not surfaced.

### Vault source-of-truth

New folder `vault/infrastructure/` mirroring the site taxonomy. Files:

- `vault/infrastructure/generalstaff.md` — positioning + status + Hard Rules + launch-plan constraints. DO NOT publish list enumerated.
- `vault/infrastructure/raybrain.md` — architecture + four invariants + refusal stance. DO NOT publish list explicitly forbids §0 content.

`vault/index.md` updated with the new Infrastructure section.

### Decision record

`vault/decisions/2026-04-18-landing-redesign-anchor.md` — successor to design-system-v1.md. Captures the taxonomy change, the anchor-vocabulary table, the accent-scarcity cardinality rule (exactly two uses on landing), the olive-dot rejection reasoning, and the "what would earn a second anchor brief" trigger list.

### CLAUDE.md un-staled

The "Current status (as of 2026-04-15)" section still claimed the project was at scaffolding stage. Updated to reflect reality: 15 pages live, anchor-v2 vocabulary layered on design-system-v1 tokens, Infrastructure taxonomy introduced, GS + RB pages shipped.

## Files changed

```
CLAUDE.md                                               # Current status block un-staled
docs/phase-22-anchor/                                   # CD anchor artifact + uploads
src/components/SiteNav.astro                            # grouped sidebar, flat topbar
src/data/navigation.ts                                  # flat → grouped
src/pages/generalstaff.astro                            # new
src/pages/index.astro                                   # full rewrite to anchor vocabulary
src/pages/raybrain.astro                                # new
src/styles/global.css                                   # anchor component classes appended
vault/decisions/2026-04-18-landing-redesign-anchor.md   # new
vault/index.md                                          # Infrastructure section added
vault/infrastructure/generalstaff.md                    # new
vault/infrastructure/raybrain.md                        # new
vault/sessions/2026-04-18-session-05.md                 # this file
```

## Open items after this session

1. **Visual walkthrough** — Ray to spot-check the 12 existing pages against the grouped sidebar nav (most likely regression area: vertical density on short-viewport screens). Dev server running at `localhost:4321`.
2. **Optional surgical upgrades** — two places where anchor vocabulary would be a clear win on existing pages:
   - `/writing` index could swap to `.entries` pattern
   - `/conflict-simulations-llc` credential strip could swap to `.infra-meta` styling
   Neither is urgent; both are judgment calls Ray should make after eyeballing the live output.
3. **Blog entry on Infrastructure** (task 11, scope confirmed shape-B "claims-first"). Will pair with the Hammerstein essay as the lived-practice companion. Drafted after Ray reviews `/generalstaff` + `/raybrain`.
4. **OG-image polish** — carried from session 4. Per-page variants + Plex fonts in the template. Still polish, still not blocking.
5. **GeneralStaff repo-public-flip task** — when `github.com/lerugray/generalstaff` goes public, swap `/generalstaff` page's "Repo currently private" line for a live link, and swap the landing infra-meta "Phases 1–4 shipped" status value for the MIT+GitHub link.

## Handoff for next session

> Continuing personal-site from session 5 (2026-04-18). Please read `CLAUDE.md`, `vault/index.md`, `vault/decisions/2026-04-18-landing-redesign-anchor.md`, `vault/sessions/2026-04-18-session-05.md` to orient. Fifteen pages live post-Phase-22. Landing redesigned via Claude Design anchor + Addendum-3 extension discipline. New Infrastructure taxonomy added with `/generalstaff` + `/raybrain`. Open items: visual walkthrough verification, two optional surgical upgrades on `/writing` index and CSL umbrella, blog-entry draft on Infrastructure (shape-B claims-first). Dev server at `localhost:4321` if not already running.
