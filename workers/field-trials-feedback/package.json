{
  "name": "field-trials-feedback",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create field-trials-feedback",
    "db:schema:remote": "wrangler d1 execute field-trials-feedback --remote --file=./schema.sql",
    "db:schema:local": "wrangler d1 execute field-trials-feedback --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.90.0"
  }
}
